<div class="caro" >
  <button type="button" class="btn btn-light explore-btn"><a class="border-a" href="#">EXPLORE</a></button> 
  <%= image_tag("mainback2.jpg", :alt => "rss feed",  :class => "carimage") %>
</div>
<div class="main-home">

    <div class="band">
      <div class="row band-row">
        <div class="col-4 band-col"><h3>EXPLORE</h3></div>
        <div class="col-4 band-col"><h3>COOK</h3></div>
        <div class="col-4 band-col"><h3>REPEAT</h3></div>
      </div>
    </div>

    <div class="category-block">
        <div class="row ">
          <% @categories.each do |category| %>
              <div class="col-md-12 col-sm-12 col-lg-4 border">
                <div>
                <%= link_to image_tag(category.category_image), category_path(category) %>
                </div>
                <div>
                 <%= link_to category.category_name, category_path(category, :title => category.category_name), :class => "home-head" %>
                </div>
              </div>
          <% end %>
        </div>
    </div>

    <div class="band center-band-head">
      <div>
        <h3 class="band-heads">OUR RECOMMENDATIONS</h3>
      </div>    
    </div>
</div>
<footer>
<div>
<% if user_signed_in? %>
        <li><%= link_to "Logout", destroy_user_session_path, :method => :delete %></li>
        <% else %>
        <li><%= link_to "Sign up now!",  new_user_registration_path%></li>
        <% end %></div></footer>


